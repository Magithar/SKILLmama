{
  "name": "skillmama-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace packages/core && npm run build --workspace packages/cli",
    "test": "npm test --workspace packages/core && npm test --workspace packages/cli"
  }
}
